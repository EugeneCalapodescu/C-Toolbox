#ifndef _PATH_H_
#define _PATH_H_

#include "Graph.h"
#include <string>

// checks if a connection from a to b exixts
bool path(const Graph& graph, const std::string& a, const std::string& b);

#endif


